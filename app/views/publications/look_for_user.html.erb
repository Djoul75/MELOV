<% if params[:query].present? %>
  <h1 class="page-header">Buddy matching whith "<%= params[:query] %>"</h1>
  <% match_users = @users.where("email LIKE ?", "%#{params[:query]}%") %>
  <% match_users.each do |u| %>
    <li><%= u.nickname %> <%= link_to 'See profile', user_path(u.id)%></li>
  <% end %>
<% end %>
